apiVersion: v1
data:
  password: {{ .Values.bigip.password | b64enc | quote }}
  username: {{ .Values.bigip.username | b64enc | quote }}
kind: Secret
metadata:
  name: bigip-login
  namespace: {{ .Release.Namespace }}
  labels:
    release: {{ .Release.Name }}
type: Opaque
